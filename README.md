# Male vs Female Classifier (HOG + SVM with OpenCV)

## Quick Setup

Install the required libraries and run the scripts in order:

```bash
# 1. Install Required Libraries
pip install numpy opencv-python scikit-learn pillow

# 2. Run the training script first
python train_hog_svm.py

# 3. Then run the GUI
python gui_hog_svm.py
